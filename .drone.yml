kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: node:18-alpine
    commands:
      - npm run lint
  - name: install dependencies
    image: node:18-alpine
    commands:
      - npm install
  - name: deploy
    image: node:18-alpine
    commands:
      - npm run start
